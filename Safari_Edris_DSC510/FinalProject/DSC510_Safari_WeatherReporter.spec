# -*- mode: python ; coding: utf-8 -*-

block_cipher = None


a = Analysis(['DSC510_Safari_WeatherReporter.py'],
             pathex=['C:\\Users\\safar\\Documents\\GitHub\\DSC-510-Fall2019\\Safari_Edris_DSC510\\FinalProject'],
             binaries=[],
             datas=[],
             hiddenimports=['tkinter'],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher,
             noarchive=False)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          a.binaries,
          a.zipfiles,
          a.datas,
          [],
          name='DSC510_Safari_WeatherReporter',
          debug=False,
          bootloader_ignore_signals=False,
          strip=False,
          upx=True,
          upx_exclude=[],
          runtime_tmpdir=None,
          console=False )
